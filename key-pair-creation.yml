- name: create a new key pair, returns generated private key
  hosts: localhost
  vars:
    ansible_python_interpreter: "/usr/bin/python3.9"
  tasks:
  - name: create a key pair
    amazon.aws.ec2_key:
      name: application_key
    no_log: true
    register: aws_ec2_key_pair